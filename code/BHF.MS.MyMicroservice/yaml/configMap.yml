apiVersion: v1
kind: ConfigMap
metadata:
  name: new8-configmap
data:
  appsettings.Production.json: |-
    {
      "ConnectionStrings": {
        "KeyVaultUri": "#{KeyVaultUri}#",
        "ManagedIdentityClientId": "#{ManagedIdentityClientId}#"
      }
    }
